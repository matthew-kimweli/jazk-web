import { FormatTimePipePipe } from './format-time-pipe.pipe';

describe('FormatTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
