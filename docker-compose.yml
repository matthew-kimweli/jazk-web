version: '3.8'

services:
  app:
    container_name: jazke-web
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3100:3100"
    environment:
      NODE_ENV: production
      DATABASE_URI: postgres://admin:admin@143.198.68.104:5432/jazke
      PUPPETEER_EXECUTABLE_PATH: /usr/bin/chromium-browser
    # volumes:
    #   - .:/usr/src/app
    # working_dir: /usr/src/app
